appId: com.warroom.app
productName: Warroom
directories:
  output: dist
  buildResources: resources
files:
  - out/**
  - package.json
# Offline dictation (Beta) uses @huggingface/transformers, which pulls in
# onnxruntime-node — a native addon (.node binary). Native addons can't be
# dlopen'd from inside an asar archive, so they need to be unpacked onto the
# real filesystem at install time.
asarUnpack:
  - node_modules/onnxruntime-node/**
extraResources:
  - from: electron/skills
    to: skills
  - from: electron/prompts
    to: prompts
  - from: resources/icon.png
    to: icon.png
  - from: resources/icon.ico
    to: icon.ico
mac:
  target:
    - target: dmg
      arch: [arm64, x64]
  category: public.app-category.productivity
  artifactName: ${productName}-${version}-${arch}.${ext}
win:
  icon: resources/icon.ico
  target:
    - target: nsis
      arch: [x64]
  artifactName: ${productName}-Setup-${version}.${ext}
protocols:
  - name: Warroom
    schemes:
      - warroom
    role: Viewer
fileAssociations:
  - ext: docx
    name: Word Document
    mimeType: application/vnd.openxmlformats-officedocument.wordprocessingml.document
    role: Viewer
    isPackage: false
  - ext: xlsx
    name: Excel Spreadsheet
    mimeType: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
    role: Viewer
    isPackage: false
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
