// PLACEHOLDER — replaced by the Auto Flow feature build.
// Upload one or many .docx speech docs; Warroom AI reads only tags + cites
// (never card bodies, via speechdoc:extractBlocks) and sorts them into the
// right speech column and flow tab, asking a clarifying question via
// <AIQuestionPrompt> when genuinely ambiguous instead of guessing.
export default function AutoFlow({ onClose }: { onClose: () => void }) {
  return null;
}
